// src/utils/errorHandler.js

export const handleError = (error) => {
    console.error('API Error:', error);
    // You can extend this function to show user-friendly messages or handle errors in other ways.
};
